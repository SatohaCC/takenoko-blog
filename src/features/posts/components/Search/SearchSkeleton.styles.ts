import { css } from '../../../../../styled-system/css';
import { stack } from '../../../../../styled-system/patterns';

export const skeletonListStyles = stack({ gap: '6' });
export const skeletonCardStyles = css({ h: '40', borderRadius: 'card' });
