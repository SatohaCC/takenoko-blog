import { css } from '../../../../styled-system/css';

export const lightWrapperStyles = css({ _dark: { display: 'none' } });
export const darkWrapperStyles = css({ display: 'none', _dark: { display: 'block' } });
export const iconImageStyles = css({ w: '20px', h: '20px' });
